stores:
  - slug: "mt-horeb"
    brand: "culvers"
    name: "Mt. Horeb"
    role: "primary"
  - slug: "madison-todd-drive"
    brand: "culvers"
    name: "Madison Todd Dr"
    role: "secondary"

# Worker API base (single source of truth for flavor data)
worker_base: "https://custard.chriskaschner.com"

google_calendar:
  calendar_id: "YOUR_CALENDAR_ID@group.calendar.google.com"
  timezone: "America/Chicago"
  event_color_id: "9"  # Optional

tidbyt:
  device_id: "your-tidbyt-device-id"
  installation_id: "culvers"
  view_mode: "three_day"  # "single" or "three_day"
  brand: "culvers"  # culvers, kopps, gilles, hefners, kraverz, oscars, generic

scheduling:
  cron_schedule: "0 6 * * *"  # Daily at 6 AM

monitoring:
  healthcheck_url: ""  # Optional: healthchecks.io ping URL
  alert_email: ""      # Optional: email for alerts
