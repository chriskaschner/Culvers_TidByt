---
phase: 21-card-button-unification
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - custard-calendar/docs/style.css
  - custard-calendar/docs/index.html
  - custard-calendar/docs/compare.html
  - custard-calendar/docs/map.html
  - custard-calendar/docs/fun.html
  - custard-calendar/docs/group.html
  - custard-calendar/docs/updates.html
  - custard-calendar/docs/forecast-map.html
  - custard-calendar/docs/planner.html
  - custard-calendar/docs/js/todays-drive.js
  - custard-calendar/docs/js/today-page.js
  - custard-calendar/docs/js/planner-domain.js
autonomous: true
requirements: [CARD-01, CARD-04]

must_haves:
  truths:
    - "All 27 card-like elements have the .card class in their HTML markup or JS-generated markup"
    - "Standalone card CSS rules no longer duplicate background, border, or border-radius properties already inherited from .card base"
    - "Page-scoped card styles in group.html and fun.html are migrated to style.css with token-based colors"
    - "Fronts overlay cards use .card.card--overlay modifier instead of standalone definitions"
    - "Cards with border-left accents use .card--accent modifier"
  artifacts:
    - path: "custard-calendar/docs/style.css"
      provides: "Deduplicated card CSS with .card inheritance"
    - path: "custard-calendar/docs/group.html"
      provides: "vote-card, winner-card, share-panel with .card class"
    - path: "custard-calendar/docs/fun.html"
      provides: "madlibs-card, linkout-card with .card class"
  key_links:
    - from: "custard-calendar/docs/style.css (.card base)"
      to: "all HTML files with card elements"
      via: "class='card {component-class}'"
      pattern: "class=\"[^\"]*card[^\"]*\""
    - from: "custard-calendar/docs/js/todays-drive.js"
      to: "custard-calendar/docs/style.css"
      via: "JS innerHTML with card class"
      pattern: "class=['\"].*card.*['\"]"
---

<objective>
Migrate all 17 standalone card patterns to inherit the .card base class -- update HTML class attributes, deduplicate CSS properties, and move page-scoped card styles into style.css.

Purpose: Achieves CARD-01 (all card elements share .card base) and partial CARD-04 (JS-generated card markup uses CSS classes). After this plan, there is ONE place to change card visual properties instead of 27.
Output: All card-like elements across all pages inherit .card, standalone card CSS deduplicated, page-scoped card styles migrated to style.css.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-card-button-unification/21-CONTEXT.md
@.planning/phases/21-card-button-unification/21-RESEARCH.md
@.planning/phases/21-card-button-unification/21-01-SUMMARY.md

<interfaces>
<!-- Plan 01 creates these card modifiers in style.css -->
.card--accent { border-left: 4px solid var(--brand); }
.card--accent-sm { border-left: 3px solid var(--brand); }
.card--compact { padding: var(--space-3); }
.card--success { border-color: var(--state-success); background: var(--state-success-bg); }
.card--danger { border-color: var(--state-danger); background: var(--state-danger-bg); }
.card--overlay { /* dark glass-morphism theme for fronts */ }

<!-- 10 cards already using .card base (no changes needed in HTML): -->
today-card (index.html:102): class="card card--hero today-card"
card--compare-day (style.css:119): class="card card--compare-day"
card--map-store (map.html:1034): class="card card--map-store store-card"
card--quiz (fun.html:158-178): class="card card--quiz quiz-mode-card" (6 instances)
blackberry-preview cards: class="card"

<!-- 17 standalone cards to migrate (from RESEARCH.md): -->
store-card: style.css:883 -- needs .card added, remove background/border duplication
day-card: style.css:1038 -- needs .card .card--accent, keep border-left accent
signal-card: style.css:1276 -- needs .card .card--accent, unique background
historical-context-card: style.css:1307 -- needs .card
near-me-card: style.css:1668 -- needs .card
today-card-skeleton: style.css:1849 -- needs .card
week-day-card: style.css:1891 -- needs .card .card--accent
updates-cta-card: style.css:2009 -- needs .card
calendar-cta-card: style.css:2048 -- needs .card .card--accent
fronts-overlay-card: style.css:2335 -- needs .card .card--overlay
fronts-legend-card: style.css:2346 -- needs .card .card--overlay (extends fronts-overlay-card)
fronts-timeline-card: style.css:2387 -- needs .card .card--overlay
error-card: style.css:2646 -- needs .card .card--danger
drive-card: style.css:2921 -- needs .card .card--accent .card--compact
compare-day-card: style.css:3508 -- needs .card
vote-card: group.html:81 page-scoped -- needs .card, move to style.css
winner-card: group.html:149 page-scoped -- needs .card .card--success, move to style.css
madlibs-card: fun.html:79 page-scoped -- needs .card, move to style.css
linkout-card: fun.html:109 page-scoped -- needs .card, move to style.css
share-panel: group.html:171 page-scoped -- needs .card, move to style.css
first-visit-prompt: style.css:3325 -- needs .card
compare-add-hint: style.css:3579 -- needs .card (dashed border preserved via modifier)
compare-nudge: style.css:3597 -- needs .card
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 17 standalone card CSS rules to inherit .card base</name>
  <files>custard-calendar/docs/style.css, custard-calendar/docs/group.html, custard-calendar/docs/fun.html</files>
  <action>
For each of the 17 standalone card patterns, perform a two-step migration:

**Step A -- CSS deduplication in style.css:**
For each standalone card rule, REMOVE properties already provided by .card base (background: var(--bg-surface), border: 1px solid var(--border), border-radius: 12px, padding: var(--space-4), box-shadow: var(--shadow-sm)) UNLESS the card intentionally overrides them. Keep only properties that differ from .card defaults or are unique to the component.

For cards that need specific overrides, keep the override and add a comment like `/* override .card base */`.

Specific card-by-card rules:

1. **store-card** (style.css:883): Remove background, border. Keep border-radius: 6px as override if design wants 6px (or remove to inherit 12px). Keep padding: var(--space-3) var(--space-4) as override.
2. **day-card** (style.css:1038): Remove background, border (except border-left). Use .card--accent for border-left. Keep specific padding if different.
3. **signal-card** (style.css:1276): Remove redundant background/border. Keep unique background (#fcfdff) as override. Use .card--accent for border-left.
4. **historical-context-card** (style.css:1307): Remove shared properties. Keep unique border/bg colors.
5. **near-me-card** (style.css:1668): Remove shared properties. Keep flex display, hover interaction.
6. **today-card-skeleton** (style.css:1849): Remove shared properties. Keep loading-state-specific border-radius if 8px intentional.
7. **week-day-card** (style.css:1891): Remove shared properties. Use .card--accent for border-left. Keep fixed width 140px.
8. **updates-cta-card** (style.css:2009): Remove shared properties. Keep flex column layout, unique bg (#f8f9fa -> use var(--bg-muted) token).
9. **calendar-cta-card** (style.css:2048): Remove shared properties. Use .card--accent for border-left.
10. **fronts-overlay-card** (style.css:2335): This card uses .card--overlay modifier (already defined in Plan 01). Remove standalone position/border/bg/shadow rules from fronts-overlay-card, since .card--overlay provides them. Keep only fronts-specific overrides (padding, width).
11. **fronts-legend-card** (style.css:2346): Remove properties already inherited from fronts-overlay-card + .card--overlay.
12. **fronts-timeline-card** (style.css:2387): Same as fronts-legend-card.
13. **error-card** (style.css:2646): Use .card--danger modifier. Remove border/bg that match .card--danger.
14. **drive-card** (style.css:2921): Use .card--accent .card--compact modifiers. Remove shared properties.
15. **compare-day-card** (style.css:3508): Remove box-shadow (inherited), border-radius (inherited if 12px matches), background (if white = var(--bg-surface)).
16. **first-visit-prompt** (style.css:3325): Remove shared properties. Keep bg-muted, rounded overrides.
17. **compare-add-hint** (style.css:3579): Keep dashed border (overrides .card border). Add .card base.
18. **compare-nudge** (style.css:3597): Keep state-watch accent.

**Step B -- Migrate page-scoped styles:**
Move card styles from group.html and fun.html <style> blocks into style.css:
- vote-card (group.html:81) -> style.css, replace hardcoded hex with tokens
- winner-card (group.html:149) -> style.css as .winner-card with .card--success
- share-panel (group.html:171) -> style.css
- madlibs-card (fun.html:79) -> style.css, replace hardcoded hex with tokens
- linkout-card (fun.html:109) -> style.css

When moving page-scoped styles, REMOVE them from the page-scoped <style> block to avoid duplication. Replace hardcoded hex values with design tokens:
- #2e7d32 -> var(--state-success) or var(--state-success-text)
- #c62828 -> var(--state-danger)
- #005696 -> var(--brand)
- #e8f5e9 -> var(--state-success-bg)
- #f8f9fa -> var(--bg-muted)

IMPORTANT: Do NOT remove non-card styles from page-scoped blocks. Only remove card-related rules that have been migrated to style.css.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && uv run pytest tests/test_design_tokens.py tests/test_card_button_unification.py -v && cd worker && npm test</automated>
  </verify>
  <done>All 17 standalone card CSS rules deduplicated to remove properties inherited from .card base. Page-scoped card styles from group.html and fun.html migrated to style.css with token-based colors. No card-related page-scoped styles remain duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: Add .card class to all card HTML elements and JS-generated markup</name>
  <files>custard-calendar/docs/index.html, custard-calendar/docs/compare.html, custard-calendar/docs/map.html, custard-calendar/docs/fun.html, custard-calendar/docs/group.html, custard-calendar/docs/updates.html, custard-calendar/docs/forecast-map.html, custard-calendar/docs/planner.html, custard-calendar/docs/js/todays-drive.js, custard-calendar/docs/js/today-page.js, custard-calendar/docs/js/planner-domain.js</files>
  <action>
For each of the 17 standalone card patterns, add "card" to the element's class list in the HTML markup or JS-generated HTML string. Also add relevant modifier classes (card--accent, card--compact, card--success, card--danger, card--overlay).

Specific element-by-element changes:

**HTML files:**
1. store-card elements: Add "card" to class. Example: class="store-card" -> class="card store-card"
2. day-card elements: Add "card card--accent". Example: class="day-card" -> class="card card--accent day-card"
3. signal-card elements: Add "card card--accent"
4. historical-context-card: Add "card"
5. near-me-card: Add "card"
6. today-card-skeleton: Add "card"
7. week-day-card: Add "card card--accent"
8. updates-cta-card: Add "card"
9. calendar-cta-card: Add "card card--accent"
10. fronts-overlay-card: Add "card card--overlay"
11. fronts-legend-card: Add "card card--overlay"
12. fronts-timeline-card: Add "card card--overlay"
13. error-card: Add "card card--danger"
14. drive-card: Add "card card--accent card--compact"
15. compare-day-card: Add "card"
16. vote-card: Add "card"
17. winner-card: Add "card card--success"
18. madlibs-card: Add "card"
19. linkout-card: Add "card"
20. share-panel: Add "card"
21. first-visit-prompt: Add "card"
22. compare-add-hint: Add "card"
23. compare-nudge: Add "card"

**JS files:**
- todays-drive.js: Find innerHTML/className assignments that create drive-card elements. Add "card card--accent card--compact" to the class string.
- today-page.js: Find innerHTML that creates today-card-skeleton or error-card. Add "card" (or "card card--danger" for error-card).
- planner-domain.js: Find any card-like elements. Add "card" class.
- compare-page.js: Find compare-day-card or compare-add-hint creation. Add "card" class. (compare-page.js is in files_modified for Plan 03 since it also has button concerns, but card classes should be added here.)

Use grep to find each card class in HTML and JS files before modifying to ensure complete coverage. After changes, grep again to verify no card elements were missed.

IMPORTANT: For elements that already have "card" in their class list (the 10 existing ones), do NOT duplicate it. Only add to the 17 standalone patterns.

For fronts overlay cards, the element may have position/z-index styles inline -- these should be preserved since .card--overlay provides default positioning but the element may need specific top/left/right values.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && uv run pytest tests/test_card_button_unification.py -v -k "card" && cd worker && npx playwright test test/browser/vizp-card-system.spec.mjs --workers=1</automated>
  </verify>
  <done>All 27 card-like elements across all pages have "card" in their class attribute. JS-generated card markup includes "card" class. vizp-card-system browser tests pass (no visual regressions on cards).</done>
</task>

</tasks>

<verification>
cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && uv run pytest tests/test_card_button_unification.py tests/test_design_tokens.py -v && cd worker && npx playwright test test/browser/vizp-card-system.spec.mjs --workers=1 && npm test
</verification>

<success_criteria>
- All 27 card elements have .card in their class attribute (HTML and JS-generated)
- Standalone card CSS rules no longer duplicate background, border, border-radius, box-shadow inherited from .card
- Page-scoped card styles from group.html and fun.html moved to style.css with design tokens
- Fronts overlay cards use .card.card--overlay modifier
- Border-left accent cards use .card--accent modifier
- All existing tests pass (no regressions)
- vizp-card-system browser tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-card-button-unification/21-02-SUMMARY.md`
</output>
