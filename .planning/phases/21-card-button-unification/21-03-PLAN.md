---
phase: 21-card-button-unification
plan: 03
type: execute
wave: 3
depends_on: [21-01, 21-02]
files_modified:
  - custard-calendar/docs/style.css
  - custard-calendar/docs/compare.html
  - custard-calendar/docs/group.html
  - custard-calendar/docs/fun.html
  - custard-calendar/docs/quiz.html
  - custard-calendar/docs/map.html
  - custard-calendar/docs/index.html
  - custard-calendar/docs/js/shared-nav.js
  - custard-calendar/docs/js/compare-page.js
  - custard-calendar/docs/js/todays-drive.js
  - custard-calendar/tests/test_card_button_unification.py
autonomous: true
requirements: [CARD-02, CARD-03, CARD-04]

must_haves:
  truths:
    - "Only 3 button base types exist: .btn-primary, .btn-secondary, .btn-text -- all domain-specific button classes remapped or removed"
    - "Zero inline style attributes set button properties (padding, color, background, border, width, display) on any button element in HTML"
    - "Zero inline style attributes set button properties in JS innerHTML strings"
    - "JS-generated button markup uses CSS class names (.btn-primary, .btn--block, etc.) not hardcoded style strings"
  artifacts:
    - path: "custard-calendar/docs/style.css"
      provides: "Consolidated button system with only 3 base types + modifiers"
    - path: "custard-calendar/docs/compare.html"
      provides: "Zero inline button styles, hardcoded button at line 73 replaced with .btn-primary"
    - path: "custard-calendar/docs/group.html"
      provides: "All button inline styles replaced with .btn--block and parent spacing"
    - path: "custard-calendar/tests/test_card_button_unification.py"
      provides: "Tightened inline style baseline tests (count = 0)"
  key_links:
    - from: "custard-calendar/docs/style.css (.btn-primary, .btn-secondary, .btn-text)"
      to: "all HTML/JS files with button elements"
      via: "class attributes replacing inline styles"
      pattern: "class=\"[^\"]*btn-(primary|secondary|text)[^\"]*\""
    - from: "custard-calendar/docs/js/shared-nav.js"
      to: "custard-calendar/docs/style.css"
      via: "CSS classes instead of inline styles for footer links"
      pattern: "btn-text|btn-primary"
---

<objective>
Consolidate the button system from 21 classes to 3 base types + modifiers, and eliminate all inline style overrides on buttons across HTML and JS files.

Purpose: Achieves CARD-02 (3 button base types), CARD-03 (zero inline button styles), and completes CARD-04 (JS innerHTML uses CSS classes). After this plan, every button in the codebase uses one of three base classes with optional modifiers.
Output: Consolidated button CSS, zero inline button styles, updated JS-generated markup, tightened test baselines to 0.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-card-button-unification/21-CONTEXT.md
@.planning/phases/21-card-button-unification/21-RESEARCH.md
@.planning/phases/21-card-button-unification/21-01-SUMMARY.md
@.planning/phases/21-card-button-unification/21-02-SUMMARY.md

<interfaces>
<!-- Plan 01 creates these button definitions and modifiers -->
.btn-text { /* no bg, no border, brand color, underline, 44px min-height */ }
.btn--block { display: block; width: 100%; }
.btn--sm { padding: var(--space-2) var(--space-3); font-size: 0.875rem; }
.btn--danger { background: var(--state-danger); color: white; }
.btn--muted { opacity: 0.6; }
.btn--icon { /* 36x36 icon button */ }
.btn--circle { border-radius: 50%; }

<!-- Button migration map from RESEARCH.md -->
.btn-google -> .btn-primary (or .btn-primary.btn--google if keeping brand distinction)
.btn-apple -> .btn-primary.btn--dark (dark bg variant)
.btn-search -> .btn-primary.btn--block
.btn-retry -> .btn-primary.btn--danger
.drive-btn -> .btn-secondary.btn--sm
.drive-btn-muted -> .btn-secondary.btn--sm.btn--muted
.calendar-cta-btn -> .btn-primary.btn--sm
.change-store-btn -> .btn-text
.store-change-btn -> .btn-text
.compare-add-hint-btn -> .btn-text
.location-geo-btn -> .btn-secondary.btn--icon
.icon-btn -> .btn-secondary.btn--icon (or .btn--circle)
.fronts-play-btn -> .btn--circle (custom dark theme)
.first-visit-confirm -> .btn-primary.btn--sm
.directions-btn (group.html) -> .btn-primary.btn--block
.quiz-geo-btn (quiz.html) -> .btn-secondary
.vote-btn -> Keep as specialized component (not in 3-type system)
.view-mode-btn -> Keep as specialized toggle component
.brand-chip -> Keep as filter chip (not a button type)

<!-- Inline style inventory from RESEARCH.md -->
compare.html:37 -- style="margin-top:1rem;" on .btn-primary
compare.html:73 -- fully hardcoded button (display, padding, bg, color, radius, weight)
compare.html:70 -- layout styles on .updates-cta-card (not button, handled in Plan 02)
group.html:254 -- style="width:100%;margin-top:0.5rem;" on .btn-primary
group.html:266 -- style="margin:0.25rem auto;" on .btn-secondary
group.html:274 -- style="width:100%;" on .btn-primary
group.html:281 -- style="display:block;width:100%;margin-top:0.75rem;" on .btn-secondary
shared-nav.js:153, 169-172, 572 -- inline styles on nav/footer elements
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remap domain-specific button CSS to base types + modifiers</name>
  <files>custard-calendar/docs/style.css, custard-calendar/docs/group.html, custard-calendar/docs/fun.html, custard-calendar/docs/quiz.html</files>
  <action>
Remap all domain-specific button classes in style.css to use the 3 base types + modifiers. For each button class:

**Phase A -- CSS consolidation in style.css:**

1. **.btn-google** (~style.css:351): This is identical to .btn-primary. Remove the standalone .btn-google definition. Add a comment: `/* .btn-google -> .btn-primary (Phase 21 consolidation) */`
2. **.btn-apple** (~style.css:360): Add .btn--dark modifier if not already present: `{ background: var(--text); color: var(--bg-surface); }` and `.btn--dark:hover { opacity: 0.85; }`. Remove standalone .btn-apple.
3. **.btn-search** (~style.css:716): This is .btn-primary.btn--block. Remove standalone definition.
4. **.btn-retry** (~style.css:2660): This is .btn-primary.btn--danger. Remove standalone definition (it was already tokenized in Phase 20).
5. **.drive-btn** (~style.css:2807): This is .btn-secondary.btn--sm. Remove standalone definition.
6. **.drive-btn-muted** (~style.css:2823): This is .btn-secondary.btn--sm.btn--muted. Remove standalone definition.
7. **.calendar-cta-btn** (~style.css:2087): This is .btn-primary.btn--sm. Remove standalone definition.
8. **.change-store-btn** (~style.css:1500): This is .btn-text. Remove standalone definition.
9. **.store-change-btn** (~style.css:3303): This is .btn-text. Remove standalone definition.
10. **.compare-add-hint-btn** (~style.css:3588): This is .btn-text. Remove standalone definition.
11. **.location-geo-btn** (~style.css:1425): This is .btn-secondary.btn--icon. Remove standalone definition.
12. **.icon-btn** (~style.css:662): This is .btn-secondary.btn--icon.btn--circle. Remove standalone definition.
13. **.fronts-play-btn** (~style.css:2397): This has a gradient background and is a circle. Create .btn--circle variant. Keep fronts-play-btn as a modifier for the gradient: `.fronts-play-btn { background: linear-gradient(...); }`. Update to extend .btn--icon.btn--circle.
14. **.first-visit-confirm** (~style.css:3344): This is .btn-primary.btn--sm. Remove standalone definition.

For .btn (the generic base at style.css:341): Keep it but add a comment noting it serves as a base for .btn-google/.btn-apple calendar subscribe buttons. If .btn-google and .btn-apple are removed, check if .btn is still referenced anywhere. If not, remove it too.

**Special cases (keep as-is, not remapped):**
- .vote-btn -- specialized toggle selector in group.html, not a standard button
- .view-mode-btn -- segmented toggle control, not a standalone button
- .brand-chip -- filter chip, not a button

**Phase B -- Migrate page-scoped button styles:**
- group.html: Move .directions-btn and any button overrides from page-scoped <style> into style.css. .directions-btn -> .btn-primary.btn--block.
- quiz.html: Move .quiz-geo-btn from page-scoped <style> into style.css. .quiz-geo-btn -> .btn-secondary.
- fun.html: Check for any button styles in page-scoped <style> block. Migrate to style.css if found.

When removing domain-specific button CSS rules, ensure no media queries or :hover/:focus states referencing the old class names are left orphaned.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && uv run pytest tests/test_card_button_unification.py -v -k "button" && uv run pytest tests/test_design_tokens.py -v && cd worker && npm test</automated>
  </verify>
  <done>Only .btn-primary, .btn-secondary, .btn-text exist as button base types in style.css (plus modifiers .btn--*). All domain-specific button classes removed or remapped. Page-scoped button styles migrated. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update HTML/JS class attributes and eliminate inline button styles</name>
  <files>custard-calendar/docs/compare.html, custard-calendar/docs/group.html, custard-calendar/docs/fun.html, custard-calendar/docs/quiz.html, custard-calendar/docs/map.html, custard-calendar/docs/index.html, custard-calendar/docs/js/shared-nav.js, custard-calendar/docs/js/compare-page.js, custard-calendar/docs/js/todays-drive.js, custard-calendar/tests/test_card_button_unification.py</files>
  <action>
**Phase A -- Update HTML class attributes to use new base + modifier classes:**

Sweep all HTML files and update button element class attributes to use the new base + modifier pattern:

1. **compare.html:37**: Remove style="margin-top:1rem;" from .btn-primary. Add margin via parent container CSS in style.css: `.updates-cta-card .btn-primary { margin-top: var(--space-4); }` (or use a spacing utility class).
2. **compare.html:73**: REMOVE the entire style="display:inline-block;padding:0.5rem 1.25rem;background:#005696;color:white;border-radius:0.5rem;text-decoration:none;font-weight:600;" attribute. Add class="btn-primary" to the element.
3. **group.html:254**: Remove style="width:100%;margin-top:0.5rem;". Change class to "btn-primary btn--block". Add margin via CSS: `#start-vote-btn { margin-top: var(--space-2); }`.
4. **group.html:266**: Remove style="margin:0.25rem auto;". Add margin via parent container CSS.
5. **group.html:274**: Remove style="width:100%;". Add btn--block to class.
6. **group.html:281**: Remove style="display:block;width:100%;margin-top:0.75rem;". Change class to "btn-secondary btn--block". Add margin via CSS.
7. All elements with class="btn-google" -> class="btn-primary" (or "btn-primary btn--google" if retaining brand marker)
8. All elements with class="btn-apple" -> class="btn-primary btn--dark"
9. All elements with class="btn-search" -> class="btn-primary btn--block"
10. All elements with class="btn-retry" -> class="btn-primary btn--danger"
11. All elements with class="drive-btn" -> class="btn-secondary btn--sm"
12. All elements with class="drive-btn-muted" -> class="btn-secondary btn--sm btn--muted"
13. All elements with class="calendar-cta-btn" -> class="btn-primary btn--sm"
14. All elements with class="change-store-btn" -> class="btn-text"
15. All elements with class="store-change-btn" -> class="btn-text"
16. All elements with class="compare-add-hint-btn" -> class="btn-text"
17. All elements with class="location-geo-btn" -> class="btn-secondary btn--icon"
18. All elements with class="icon-btn" -> class="btn-secondary btn--icon btn--circle"
19. All elements with class="first-visit-confirm" -> class="btn-primary btn--sm"
20. All elements with class="directions-btn" -> class="btn-primary btn--block"
21. All elements with class="quiz-geo-btn" -> class="btn-secondary"

Use grep to find ALL occurrences of each old class name in HTML files before modifying. Verify no old class names remain after changes (except .vote-btn, .view-mode-btn, .brand-chip which are kept).

**Phase B -- Update JS-generated markup:**

1. **shared-nav.js**: Find inline styles on footer links (lines 153, 169-172, 572). Replace inline color/padding/display styles with appropriate CSS classes. If these are links styled as buttons, use .btn-text. If they are nav links, create a CSS class in style.css for the footer link styling instead of inline styles.
2. **compare-page.js**: Find any button creation in innerHTML. Verify it uses CSS classes. If any inline styles on buttons exist, replace with classes.
3. **todays-drive.js**: Find drive-btn references in innerHTML. Update to "btn-secondary btn--sm". Find drive-btn-muted references. Update to "btn-secondary btn--sm btn--muted".

For JS files, search for patterns: `style=`, `.style.`, `style\.cssText`, `setAttribute('style'` to catch all inline style assignments. Replace with classList operations or class attribute strings.

**Phase C -- Tighten test baselines:**

Update test_card_button_unification.py:
- test_no_inline_button_styles_in_html: Change the allowed baseline count to 0
- test_no_inline_button_styles_in_js: Change the allowed baseline count to 0
- test_only_three_button_base_types: Remove legacy allowlist entries for .btn-google, .btn-apple, .btn-search, .btn-retry, etc. Only .btn-primary, .btn-secondary, .btn-text should pass.

After tightening, run the tests to confirm zero violations remain.

**Important edge cases:**
- Some elements may use the old class name as a JS selector (document.querySelector('.drive-btn')). Check for querySelector/getElementById usage of old class names in JS files. If found, either update the selector to the new class name, or keep the old class name as an additional class for JS hooks (add a data attribute instead if cleaner).
- The subscribe buttons page (updates.html or calendar page) uses .btn-google and .btn-apple. Update those to .btn-primary and .btn-primary.btn--dark.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && uv run pytest tests/test_card_button_unification.py tests/test_design_tokens.py -v && cd worker && npx playwright test test/browser/vizp-card-system.spec.mjs --workers=1 && npm test</automated>
  </verify>
  <done>Zero inline style="" attributes on button elements in HTML. Zero inline button styles in JS innerHTML. All button elements use .btn-primary, .btn-secondary, or .btn-text with modifiers. Test baselines tightened to 0 violations. All existing tests pass (no regressions).</done>
</task>

</tasks>

<verification>
cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && uv run pytest tests/test_card_button_unification.py tests/test_design_tokens.py -v && cd worker && npx playwright test test/browser/vizp-card-system.spec.mjs --workers=1 && npm test
</verification>

<success_criteria>
- Only 3 button base types remain in style.css: .btn-primary, .btn-secondary, .btn-text
- All domain-specific button classes removed or remapped to base + modifier combos
- Zero inline style="" attributes setting button properties in any HTML file
- Zero inline button styles in JS innerHTML strings
- JS-generated button markup uses CSS class names
- Test baselines tightened to 0 violations
- All existing tests pass (no visual regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/21-card-button-unification/21-03-SUMMARY.md`
</output>
