---
phase: 21-card-button-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custard-calendar/tests/test_card_button_unification.py
  - custard-calendar/docs/style.css
autonomous: true
requirements: [CARD-01, CARD-02, CARD-03]

must_haves:
  truths:
    - "Static analysis tests exist that verify card base class presence, button class consolidation, and zero inline button styles"
    - ".btn-text base type exists in style.css with no-background, brand-color, underline-on-hover properties"
    - "Button modifiers (.btn--block, .btn--sm, .btn--danger, .btn--muted, .btn--icon, .btn--circle) exist in style.css"
    - "Card modifiers (.card--accent, .card--success, .card--danger, .card--overlay, .card--compact) exist in style.css"
  artifacts:
    - path: "custard-calendar/tests/test_card_button_unification.py"
      provides: "Static analysis tests for CARD-01, CARD-02, CARD-03 requirements"
      min_lines: 80
    - path: "custard-calendar/docs/style.css"
      provides: ".btn-text, button modifiers, card modifiers"
      contains: ".btn-text"
  key_links:
    - from: "custard-calendar/tests/test_card_button_unification.py"
      to: "custard-calendar/docs/style.css"
      via: "CSS static analysis (file read + regex)"
      pattern: "test_.*card|test_.*button|test_.*inline"
---

<objective>
Create the test scaffold and CSS foundation for card/button unification -- define all new base classes and modifiers in style.css, and create the static analysis tests that will verify CARD-01, CARD-02, CARD-03 compliance.

Purpose: Establishes the CSS contracts (new base classes and modifiers) that Plans 02 and 03 will apply across HTML/JS files. Tests written first so Plans 02/03 can verify their work incrementally.
Output: test_card_button_unification.py with static analysis tests, style.css with .btn-text, button modifiers, and card modifiers.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-card-button-unification/21-CONTEXT.md
@.planning/phases/21-card-button-unification/21-RESEARCH.md
@.planning/phases/20-design-token-expansion/20-02-SUMMARY.md

<interfaces>
<!-- Existing .card base class from style.css:111-117 -->
.card {
  background: var(--bg-surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: var(--space-4);
  box-shadow: var(--shadow-sm);
}

<!-- Existing .btn base from style.css:341-349 -->
.btn {
  display: inline-block;
  padding: var(--space-3) var(--space-5);
  border-radius: 6px;
  text-decoration: none;
  font-weight: 600;
  font-size: 0.875rem;
  text-align: center;
}

<!-- Existing .btn-primary from style.css:1552-1567 -->
.btn-primary {
  display: inline-block;
  padding: var(--space-3) var(--space-6);
  background: var(--brand);
  color: var(--bg-surface);
  border: none;
  border-radius: 6px;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  text-decoration: none;
}

<!-- Existing .btn-secondary from style.css:1569-1583 -->
.btn-secondary {
  display: inline-block;
  padding: var(--space-3) 1.25rem;
  border: 1px solid var(--brand);
  color: var(--brand);
  background: var(--bg-surface);
  border-radius: 6px;
  font-size: 1rem;
  font-weight: 600;
  text-decoration: none;
}

<!-- Phase 20 state tokens available in :root -->
--state-confirmed, --state-watch, --state-danger, --state-success
--state-danger-bg, --state-success-bg, --state-danger-text, --state-success-text
--hover-bg, --focus-ring, --focus-ring-offset

<!-- Existing test pattern from test_design_tokens.py -->
Tests use: open(style_css_path, 'r') then regex matching on CSS content.
Test file location: custard-calendar/tests/test_design_tokens.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create static analysis tests for card/button unification</name>
  <files>custard-calendar/tests/test_card_button_unification.py</files>
  <behavior>
    - Test CARD-01: .card class definition exists in style.css with border-radius, box-shadow, border properties
    - Test CARD-02 (partial): .btn-text class definition exists in style.css
    - Test CARD-02 (partial): Only 3 button base types (.btn-primary, .btn-secondary, .btn-text) exist -- no other .btn-{name} class definitions that aren't modifiers (.btn--*)
    - Test CARD-03: Zero inline style="" attributes setting button properties (padding, color, background, border, width, display) in any HTML file under docs/
    - Test CARD-03: Zero inline style="" attributes setting button properties in JS innerHTML strings in docs/js/ files
    - Test: .btn--block, .btn--sm modifiers exist in style.css
    - Test: .card--accent, .card--success, .card--danger modifiers exist in style.css
  </behavior>
  <action>
Create custard-calendar/tests/test_card_button_unification.py following the existing pattern in test_design_tokens.py. Use pathlib.Path to locate custard-calendar/docs/style.css, docs/*.html, and docs/js/*.js.

Tests to create:

1. test_card_base_class_exists -- verify .card rule exists with border-radius, box-shadow, border
2. test_btn_text_base_type_exists -- verify .btn-text rule exists in style.css
3. test_only_three_button_base_types -- scan for .btn-{word} (not .btn--{word}) class definitions; only .btn-primary, .btn-secondary, .btn-text should exist as base types. Note: .btn (the generic base), .btn-google, .btn-apple may still exist during migration -- mark those as known legacy in an allowlist that shrinks as Plan 03 migrates them. The allowlist approach lets this test be useful immediately and tighten later.
4. test_no_inline_button_styles_in_html -- scan all .html files in docs/ for style="" attributes on elements that also have btn/button classes, or style="" attributes containing button properties (padding, background, border-radius) on <button> or <a> elements with button classes. Initially this test will have a count baseline (current violations) that Plan 03 drives to zero.
5. test_no_inline_button_styles_in_js -- scan docs/js/*.js for innerHTML/insertAdjacentHTML strings containing style=" with button properties. Use a count baseline approach.
6. test_button_modifiers_exist -- verify .btn--block, .btn--sm, .btn--danger exist in style.css
7. test_card_modifiers_exist -- verify .card--accent, .card--success, .card--danger, .card--overlay exist in style.css

For tests 4 and 5, use a "baseline" approach: the test asserts the count of violations is <= N, where N starts at the current count. Plan 03 will update N to 0 when it eliminates all violations. This way the test is useful immediately (prevents regressions) and tightens over time.

Follow the project's test patterns: pytest class-based grouping, pathlib for file paths, clear assertion messages.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && uv run pytest tests/test_card_button_unification.py -v</automated>
  </verify>
  <done>All tests pass. Tests for .btn-text and modifiers will initially fail (those CSS rules don't exist yet) -- that's the RED state. Task 2 makes them GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Define .btn-text, button modifiers, and card modifiers in style.css</name>
  <files>custard-calendar/docs/style.css</files>
  <action>
Add the following CSS definitions to style.css. Place them in logical sections near the existing card and button rules.

**New .btn-text base type** (place after .btn-secondary:hover, ~line 1583):
```css
.btn-text {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: none;
  border: none;
  color: var(--brand);
  font-size: var(--text-base);
  font-weight: 600;
  cursor: pointer;
  text-decoration: underline;
  padding: 0 var(--space-1);
  min-height: 44px;
}
.btn-text:hover {
  color: var(--brand-dark, var(--brand-hover));
}
```

**Button modifiers** (place after .btn-text:hover):
```css
.btn--block { display: block; width: 100%; }
.btn--sm { padding: var(--space-2) var(--space-3); font-size: 0.875rem; }
.btn--danger { background: var(--state-danger); color: white; }
.btn--danger:hover { background: var(--state-danger-text); }
.btn--muted { opacity: 0.6; }
.btn--icon {
  display: inline-flex; align-items: center; justify-content: center;
  padding: var(--space-2); min-width: 36px; min-height: 36px;
}
.btn--circle { border-radius: 50%; }
```

**Card modifiers** (place after existing .card--quiz rules, ~line 129):
```css
.card--accent { border-left: 4px solid var(--brand); }
.card--accent-sm { border-left: 3px solid var(--brand); }
.card--compact { padding: var(--space-3); }
.card--success { border-color: var(--state-success); background: var(--state-success-bg); }
.card--danger { border-color: var(--state-danger); background: var(--state-danger-bg); }
.card--overlay {
  position: absolute;
  z-index: 520;
  border: 1px solid rgba(137, 155, 185, 0.34);
  border-radius: 14px;
  background: linear-gradient(160deg, rgba(26, 33, 48, 0.88), rgba(22, 28, 44, 0.78));
  backdrop-filter: blur(7px);
  box-shadow: 0 10px 28px rgba(0, 0, 0, 0.35);
  color: #e5ebf6;
}
```

Important:
- Do NOT remove or rename any existing CSS rules in this task -- additive only
- Do NOT touch any HTML or JS files -- this task only modifies style.css
- Verify no duplicate selectors are created
- The .card--overlay values come from the existing fronts-overlay-card (style.css:2335), which will be deduplicated in Plan 02 when that card inherits .card base
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && uv run pytest tests/test_card_button_unification.py tests/test_design_tokens.py -v && cd worker && npm test</automated>
  </verify>
  <done>All new test_card_button_unification.py tests pass GREEN. Existing test_design_tokens.py still passes. All 1351+ worker unit tests pass. style.css contains .btn-text, .btn--block, .btn--sm, .btn--danger, .btn--muted, .btn--icon, .btn--circle, .card--accent, .card--compact, .card--success, .card--danger, .card--overlay.</done>
</task>

</tasks>

<verification>
cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && uv run pytest tests/test_card_button_unification.py tests/test_design_tokens.py -v && cd worker && npm test
</verification>

<success_criteria>
- test_card_button_unification.py exists with 7+ test functions covering CARD-01, CARD-02, CARD-03 requirements
- .btn-text base type defined in style.css
- 6 button modifiers defined in style.css (.btn--block, .btn--sm, .btn--danger, .btn--muted, .btn--icon, .btn--circle)
- 5+ card modifiers defined in style.css (.card--accent, .card--accent-sm, .card--compact, .card--success, .card--danger, .card--overlay)
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/21-card-button-unification/21-01-SUMMARY.md`
</output>
