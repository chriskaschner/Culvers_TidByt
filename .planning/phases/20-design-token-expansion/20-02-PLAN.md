---
phase: 20-design-token-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - custard-calendar/docs/style.css
  - custard-calendar/docs/quiz.html
  - custard-calendar/docs/forecast-map.html
autonomous: true
requirements:
  - DTKN-02
  - DTKN-04

must_haves:
  truths:
    - "Rarity badges on map popups, Today page, and Compare page all use the same purple > blue > green color scale from --rarity-* tokens"
    - "Common and staple rarity badges are hidden (display:none) in all contexts -- map popups, Today, Compare"
    - "Focus rings use var(--focus-ring) on all focusable elements via :focus-visible"
    - "Map marker glow values derive from state tokens via color-mix() instead of hardcoded rgba()"
    - "Quiz danger/success colors reference shared state tokens instead of inline overrides"
    - "Brand border classes use var(--brand-*) tokens"
  artifacts:
    - path: "custard-calendar/docs/style.css"
      provides: "Rarity badge unification, interactive token wiring, brand token wiring"
    - path: "custard-calendar/docs/quiz.html"
      provides: "Quiz tokens migrated to shared state tokens"
  key_links:
    - from: "custard-calendar/docs/style.css .popup-rarity-chip.rarity-*"
      to: "custard-calendar/docs/style.css :root --rarity-*"
      via: "var(--rarity-*-bg) and var(--rarity-*-text)"
      pattern: "var\\(--rarity-"
    - from: "custard-calendar/docs/style.css .rarity-badge-*"
      to: "custard-calendar/docs/style.css :root --rarity-*"
      via: "var(--rarity-*-bg) and var(--rarity-*-text)"
      pattern: "var\\(--rarity-"
    - from: "custard-calendar/docs/quiz.html"
      to: "custard-calendar/docs/style.css :root --state-*"
      via: "--quiz-danger: var(--state-danger-text)"
      pattern: "var\\(--state-"
    - from: "custard-calendar/docs/style.css :focus-visible"
      to: "custard-calendar/docs/style.css :root --focus-ring"
      via: "var(--focus-ring)"
      pattern: "var\\(--focus-ring"
---

<objective>
Wire rarity token unification (DTKN-02) and interactive token adoption (DTKN-04) into the codebase. Replace divergent rarity palettes with unified --rarity-* tokens, hide common/staple badges, wire focus/hover tokens, migrate quiz colors, and derive map marker glows from state tokens.

Purpose: Complete the token adoption across all three requirement areas. Plan 01 defined the tokens and did state colors; this plan wires rarity and interactive tokens.
Output: Unified rarity palette across all contexts, consistent focus rings, quiz token migration, brand border tokenization.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-design-token-expansion/20-CONTEXT.md
@.planning/phases/20-design-token-expansion/20-RESEARCH.md
@.planning/phases/20-design-token-expansion/20-01-SUMMARY.md

@custard-calendar/docs/style.css
@custard-calendar/docs/quiz.html
@custard-calendar/docs/forecast-map.html

<interfaces>
<!-- Plan 01 added these tokens to :root (available for use): -->
<!-- State: --state-confirmed, --state-confirmed-bg, --state-confirmed-text, --state-watch, --state-watch-bg, --state-watch-text, --state-watch-border, --state-estimated, --state-estimated-bg, --state-estimated-text, --state-success, --state-success-bg, --state-success-text, --state-danger, --state-danger-bg, --state-danger-text, --state-none -->
<!-- Rarity: --rarity-ultra-rare, --rarity-ultra-rare-bg, --rarity-ultra-rare-text, --rarity-rare, --rarity-rare-bg, --rarity-rare-text, --rarity-uncommon, --rarity-uncommon-bg, --rarity-uncommon-text -->
<!-- Interactive: --focus-ring, --focus-ring-offset, --hover-bg, --hover-border -->
<!-- Brand: --brand-culvers, --brand-kopps, --brand-gilles, --brand-hefners, --brand-kraverz, --brand-oscars -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify rarity badge palette and hide common/staple (DTKN-02)</name>
  <files>custard-calendar/docs/style.css</files>
  <action>
**Part A: Replace popup rarity chip colors (lines ~956-960) with --rarity-* tokens:**

```css
/* BEFORE (divergent palette) */
.popup-rarity-chip.rarity-ultra-rare { background: #fce4ec; color: #b71c1c; }
.popup-rarity-chip.rarity-rare { background: #fff3e0; color: #e65100; }
.popup-rarity-chip.rarity-uncommon { background: #e3f2fd; color: #1565c0; }
.popup-rarity-chip.rarity-common { background: #e8f5e9; color: #2e7d32; }
.popup-rarity-chip.rarity-staple { background: #f5f5f5; color: #777; }

/* AFTER (unified popup palette tokens -- solid pills, white text for top 3) */
.popup-rarity-chip.rarity-ultra-rare { background: var(--rarity-ultra-rare-bg); color: var(--rarity-ultra-rare-text); }
.popup-rarity-chip.rarity-rare { background: var(--rarity-rare-bg); color: var(--rarity-rare-text); }
.popup-rarity-chip.rarity-uncommon { background: var(--rarity-uncommon-bg); color: var(--rarity-uncommon-text); }
```

Note: Check the actual current hex values at those lines -- the RESEARCH may reference slightly different line numbers. Find the `.popup-rarity-chip.rarity-*` rules and replace them.

**Part B: Replace Today/Compare rarity badge colors (lines ~1770-1774) with same tokens:**

```css
/* BEFORE (pink/orange/blue palette) */
.rarity-badge-ultra-rare { background: #fce4ec; color: #b71c1c; }
.rarity-badge-rare { background: #fff3e0; color: #e65100; }
.rarity-badge-uncommon { background: #e3f2fd; color: #1565c0; }

/* AFTER (unified popup palette tokens -- solid pills) */
.rarity-badge-ultra-rare { background: var(--rarity-ultra-rare-bg); color: var(--rarity-ultra-rare-text); }
.rarity-badge-rare { background: var(--rarity-rare-bg); color: var(--rarity-rare-text); }
.rarity-badge-uncommon { background: var(--rarity-uncommon-bg); color: var(--rarity-uncommon-text); }
```

**Part C: Hide common and staple badges everywhere.** Add these new rules:

```css
/* Hide common and staple rarity badges -- only uncommon+ shown (user decision) */
.rarity-badge-common,
.rarity-badge-staple,
.popup-rarity-chip.rarity-common,
.popup-rarity-chip.rarity-staple {
  display: none;
}
```

**Part D: Wire brand border tokens.** Replace hardcoded hex in `.brand-border-*` rules (lines ~854-859):

```css
.brand-border-culvers { border-left-color: var(--brand-culvers); }
.brand-border-kopps { border-left-color: var(--brand-kopps); }
.brand-border-gilles { border-left-color: var(--brand-gilles); }
.brand-border-hefners { border-left-color: var(--brand-hefners); }
.brand-border-kraverz { border-left-color: var(--brand-kraverz); }
.brand-border-oscars { border-left-color: var(--brand-oscars); }
```

**Part E: Replace map marker glow rgba values with color-mix() derivations from state tokens.** Find `.flavor-map-marker-*` rules (lines ~568-580) that use `--marker-state-glow: rgba(...)` and replace with:

```css
.flavor-map-marker-match {
  --marker-state-glow: color-mix(in srgb, var(--state-success) 52%, transparent);
}
.flavor-map-marker-confirmed {
  --marker-state-glow: color-mix(in srgb, var(--state-confirmed) 37%, transparent);
}
.flavor-map-marker-watch {
  --marker-state-glow: color-mix(in srgb, var(--state-watch) 37%, transparent);
}
```

Check the actual marker class names and rgba percentages in the code -- adapt the color-mix() percentages to match the original rgba alpha values.

**CRITICAL: Do not modify the existing 37 tokens in lines 1-51 of :root.**
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npx playwright test test/browser/dtkn-rarity-tokens.spec.mjs -x --reporter=list 2>&1 | tail -10</automated>
  </verify>
  <done>Rarity badges on popup chips and Today/Compare badges use identical --rarity-* token colors. Common and staple badges hidden via display:none. Brand borders use --brand-* tokens. Map marker glows derived from state tokens via color-mix().</done>
</task>

<task type="auto">
  <name>Task 2: Wire interactive tokens and migrate quiz colors (DTKN-04)</name>
  <files>
    custard-calendar/docs/style.css,
    custard-calendar/docs/quiz.html,
    custard-calendar/docs/forecast-map.html
  </files>
  <action>
**Part A: Wire focus ring token into :focus-visible rule.** Find the existing `:focus-visible` rule (lines ~2628-2641) and update:

```css
/* BEFORE */
:focus-visible {
  outline: 2px solid var(--brand);
  outline-offset: 2px;
}

/* AFTER */
:focus-visible {
  outline: 2px solid var(--focus-ring);
  outline-offset: var(--focus-ring-offset);
}
```

**Part B: Wire hover token.** Search for hover states that use hardcoded rgba or hex backgrounds. Where appropriate, replace with `var(--hover-bg)` or `var(--hover-border)`. Use judgment -- not every hover needs the same token. Focus on interactive elements (buttons, links, cards) that should feel cohesive. Leave one-off decorative hovers alone.

**Part C: Migrate quiz danger/success to shared state tokens.** In quiz.html, find the inline `<style>` block (lines ~25-26) that defines:

```css
--quiz-danger: #b42318;
--quiz-success: #0f8a5f;
```

Replace with references to shared state tokens:

```css
--quiz-danger: var(--state-danger-text);
--quiz-success: var(--state-success-text);
```

This keeps the quiz-specific variable names (they may be used throughout quiz.html) but derives them from the shared token system. Do NOT move the entire quiz inline style block to style.css (that is DTKN-F2, a future requirement).

**Part D: Check forecast-map.html** for any inline state/rarity hex values that should reference tokens. If the forecast map has state-colored markers or badges with hardcoded hex, replace them with token references. If it only uses CSS classes (which already got tokenized in Plan 01), no changes needed.

**Part E: Drive dashboard bucket colors (Claude's discretion).** The RESEARCH mentions drive dashboard bucket colors at lines ~3112-3119 that overlap with state tokens (great=#2e7d32, ok=#f9a825). If these exist and are simple to tokenize, do it:
```css
/* great -> var(--state-success), ok -> var(--state-watch), etc. */
```
If it would require significant refactoring or the drive classes don't directly map, skip it.

**Part F: Final validation sweep.** After all changes, run a quick grep to verify no obvious state/rarity hex values remain in CSS rules (outside of :root definitions). Search for: `#f0f7ff`, `#fffde7`, `#fce4ec`, `#fff3e0`, `#e3f2fd`, `#2e7d32` (in non-:root contexts), `#f9a825` (in non-:root contexts). Some may legitimately remain if they are not state/rarity colors -- use judgment.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npx playwright test test/browser/dtkn-interactive-tokens.spec.mjs test/browser/dtkn-rarity-tokens.spec.mjs test/browser/dtkn-state-tokens.spec.mjs -x --reporter=list 2>&1 | tail -15</automated>
  </verify>
  <done>All three DTKN test files pass GREEN. Focus ring uses --focus-ring token. Quiz danger/success derive from shared state tokens. No major hardcoded state/rarity hex values remain in CSS rules outside :root. Brand borders tokenized. Map marker glows derive from state tokens.</done>
</task>

</tasks>

<verification>
1. `cd custard-calendar/worker && npx playwright test test/browser/dtkn-state-tokens.spec.mjs test/browser/dtkn-rarity-tokens.spec.mjs test/browser/dtkn-interactive-tokens.spec.mjs -x` -- all DTKN tests pass
2. `cd custard-calendar/worker && npx playwright test test/browser/vizp-card-system.spec.mjs -x` -- existing card system tests still pass
3. `cd custard-calendar/worker && npm test` -- full unit suite passes
4. `cd custard-calendar/worker && npx playwright test test/browser/vizp-seasonal-rarity.spec.mjs -x` -- existing rarity tests still pass (if they exist)
5. Visual spot check: rarity badges should show purple/blue/green (not pink/orange/blue)
</verification>

<success_criteria>
- Popup rarity chips and Today/Compare rarity badges use identical --rarity-* token values (purple > blue > green)
- Common and staple badges hidden everywhere
- :focus-visible uses var(--focus-ring) and var(--focus-ring-offset)
- Quiz --quiz-danger and --quiz-success derive from --state-danger-text and --state-success-text
- Brand borders use var(--brand-*) tokens
- Map marker glows use color-mix() with state tokens
- All 3 DTKN browser test files pass GREEN
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-design-token-expansion/20-02-SUMMARY.md`
</output>
