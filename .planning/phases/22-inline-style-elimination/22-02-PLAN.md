---
phase: 22-inline-style-elimination
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - custard-calendar/docs/style.css
  - custard-calendar/docs/compare-page.js
  - custard-calendar/docs/shared-nav.js
  - custard-calendar/docs/today-page.js
  - custard-calendar/docs/forecast-map.html
  - custard-calendar/docs/updates-page.js
  - custard-calendar/tests/test_inline_style_elimination.py
autonomous: true
requirements: [DTKN-03]

must_haves:
  truths:
    - "compare-page.js has zero .style.display assignments"
    - "shared-nav.js has zero .style.display assignments"
    - "forecast-map.html inline JS uses classList.toggle('hidden') instead of .style.display"
    - "forecast-map.html tick styling uses CSS classes instead of .style.fontWeight/.color"
    - "updates-page.js uses CSS classes for success/danger text colors instead of .style.color"
    - "Search filter in compare-page.js and shared-nav.js still shows/hides items correctly"
    - "Forecast map confirmed/forecast view mode toggle still works"
  artifacts:
    - path: "custard-calendar/docs/compare-page.js"
      provides: "classList.toggle('hidden') instead of .style.display"
    - path: "custard-calendar/docs/shared-nav.js"
      provides: "classList.toggle('hidden') instead of .style.display"
    - path: "custard-calendar/docs/forecast-map.html"
      provides: "classList.toggle for visibility, CSS classes for tick styling"
    - path: "custard-calendar/docs/updates-page.js"
      provides: "CSS class-based text colors for status messages"
    - path: "custard-calendar/docs/style.css"
      provides: ".fronts-tick--confirmed, .text-success, .text-danger classes"
    - path: "custard-calendar/tests/test_inline_style_elimination.py"
      provides: "Tests for JS .style.* elimination"
  key_links:
    - from: "custard-calendar/docs/compare-page.js"
      to: "custard-calendar/docs/style.css"
      via: "classList.toggle('hidden') uses .hidden class"
      pattern: "classList\\.toggle\\('hidden'"
    - from: "custard-calendar/docs/shared-nav.js"
      to: "custard-calendar/docs/style.css"
      via: "classList.toggle('hidden') uses .hidden class"
      pattern: "classList\\.toggle\\('hidden'"
    - from: "custard-calendar/docs/forecast-map.html"
      to: "custard-calendar/docs/style.css"
      via: "classList.toggle('hidden') and .fronts-tick--confirmed class"
      pattern: "classList\\.toggle|fronts-tick--confirmed"
---

<objective>
Convert all .style.display, .style.fontWeight, .style.color, and other .style.* assignments in JS files to use classList operations with CSS classes. This is the JS half of DTKN-03.

Purpose: Eliminates programmatic inline style assignments so all visual properties come from CSS classes, completing the DTKN-03 requirement. The .hidden class from Plan 01 enables classList.toggle('hidden') for visibility toggling.
Output: Zero .style.display in compare-page.js and shared-nav.js. Tick styling and view-mode visibility in forecast-map.html use CSS classes. updates-page.js status colors use CSS classes.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-inline-style-elimination/22-CONTEXT.md
@.planning/phases/22-inline-style-elimination/22-RESEARCH.md
@.planning/phases/22-inline-style-elimination/22-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
Plan 01 creates .hidden { display: none !important; } in style.css.
This class is required for classList.toggle('hidden', condition) pattern.

From custard-calendar/docs/style.css :root:
```css
--brand: #005696;
--state-success: #2e7d32;
--state-danger: #c62828;
```

JS patterns to convert (from RESEARCH.md):

compare-page.js (2 occurrences):
  Line 660: item.style.display = '';          -> classList.remove('hidden')
  Line 671: item.style.display = match ? '' : 'none'; -> classList.toggle('hidden', !match)

shared-nav.js (2 occurrences):
  Line 407: item.style.display = '';          -> classList.remove('hidden')
  Line 418: item.style.display = match ? '' : 'none'; -> classList.toggle('hidden', !match)

today-page.js (3 occurrences -- dynamic brand color, Claude's discretion):
  Line 360: todayCard.style.borderLeftColor = '';
  Line 362: todayFlavor.style.color = '';
  Line 366: todayCard.style.borderLeftColor = color;

forecast-map.html inline JS:
  Lines 261-262: tick.style.fontWeight/color -> classList.toggle('fronts-tick--confirmed', hasConfirmed)
  Lines 880-881, 893-894, 914-915, 917-918: .style.display -> classList.toggle('hidden')
  Lines 653, 717: style= in innerHTML strings (dynamic colors -- see discretion notes)

updates-page.js (3 occurrences -- opportunistic cleanup):
  Line 82: statusEl.style.color = '#2e7d32'; -> classList.add('text-success')
  Line 86: statusEl.style.color = '#c62828'; -> classList.add('text-danger')
  Line 91: statusEl.style.color = '#c62828'; -> classList.add('text-danger')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JS enforcement tests and new CSS classes for JS patterns</name>
  <files>custard-calendar/tests/test_inline_style_elimination.py, custard-calendar/docs/style.css</files>
  <action>
    **Add tests** to test_inline_style_elimination.py (created in Plan 01):

    1. test_no_style_display_in_compare_js: compare-page.js has zero `.style.display` assignments
    2. test_no_style_display_in_shared_nav_js: shared-nav.js has zero `.style.display` assignments
    3. test_no_inline_style_assignments_forecast_map_js: forecast-map.html inline JS has zero `.style.fontWeight`, `.style.color`, `.style.display` assignments. Use regex to search within `<script>` blocks only. Allow exception for truly dynamic values in innerHTML strings (popup family color span, hotspot-dot background) since those are dynamically generated from data -- document this exception in the test with a comment.
    4. test_no_style_color_in_updates_js: updates-page.js has zero `.style.color` assignments

    For today-page.js: Claude's discretion on whether to keep .style for truly dynamic per-store brand colors or convert to CSS custom properties. If kept, add a test documenting the exception. If converted, add enforcement test.

    **Add CSS classes** to style.css:

    1. `.fronts-tick--confirmed` -- for forecast-map.html confirmed day ticks:
       ```css
       .fronts-tick--confirmed {
         font-weight: 700;
         color: var(--brand);  /* #005696 */
       }
       ```
       Note: The default tick state (font-weight:400, color:#9fb1d1) should be set on `.fronts-tick-row span` or similar base selector. The #9fb1d1 is part of the fronts dark theme palette and is acceptable as hardcoded per the existing allowlist pattern.

    2. `.text-success` and `.text-danger` -- for updates-page.js status colors:
       ```css
       .text-success {
         color: var(--state-success);  /* #2e7d32 */
       }
       .text-danger {
         color: var(--state-danger);   /* #c62828 */
       }
       ```

    Run the new tests -- they MUST fail at this point (tests are written before the JS changes).
  </action>
  <verify>
    <automated>cd custard-calendar && uv run pytest tests/test_inline_style_elimination.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - New tests exist for JS .style.* enforcement
    - .fronts-tick--confirmed, .text-success, .text-danger classes defined in style.css
    - Tests correctly fail because JS files still use .style.* assignments
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert all JS .style.* assignments to classList operations</name>
  <files>custard-calendar/docs/compare-page.js, custard-calendar/docs/shared-nav.js, custard-calendar/docs/today-page.js, custard-calendar/docs/forecast-map.html, custard-calendar/docs/updates-page.js</files>
  <action>
    Convert each .style.* assignment to use CSS classes:

    **compare-page.js (2 changes):**
    - Line 660: `item.style.display = '';` -> `item.classList.remove('hidden');`
    - Line 671: `item.style.display = match ? '' : 'none';` -> `item.classList.toggle('hidden', !match);`

    **shared-nav.js (2 changes):**
    - Line 407: `item.style.display = '';` -> `item.classList.remove('hidden');`
    - Line 418: `item.style.display = match ? '' : 'none';` -> `item.classList.toggle('hidden', !match);`

    **forecast-map.html inline JS -- tick styling (lines 261-262):**
    Replace:
    ```javascript
    tick.style.fontWeight = hasConfirmed ? '700' : '400';
    tick.style.color = hasConfirmed ? '#005696' : '#9fb1d1';
    ```
    With:
    ```javascript
    tick.classList.toggle('fronts-tick--confirmed', hasConfirmed);
    ```
    Add a base CSS rule for default tick state if one does not exist:
    ```css
    .fronts-tick-row span {
      font-weight: 400;
      color: #9fb1d1;  /* fronts dark theme default -- not tokenized */
    }
    ```

    **forecast-map.html inline JS -- view mode visibility (8 .style.display lines):**
    Lines 880-881 (mode-confirmed click):
    ```javascript
    if (timelineCard) timelineCard.classList.add('hidden');
    if (flavorBlock) flavorBlock.classList.add('hidden');
    ```
    Lines 893-894 (mode-forecast click):
    ```javascript
    if (timelineCard) timelineCard.classList.remove('hidden');
    if (flavorBlock) flavorBlock.classList.remove('hidden');
    ```
    Lines 914-915 (applyViewModeVisibility confirmed):
    ```javascript
    if (timelineCard) timelineCard.classList.add('hidden');
    if (flavorBlock) flavorBlock.classList.add('hidden');
    ```
    Lines 917-918 (applyViewModeVisibility forecast):
    ```javascript
    if (timelineCard) timelineCard.classList.remove('hidden');
    if (flavorBlock) flavorBlock.classList.remove('hidden');
    ```

    **forecast-map.html inline JS -- innerHTML style= strings (lines 653, 717):**
    These are truly dynamic values (familyColor from data, color from store data). They CANNOT be replaced with static CSS classes because the colors vary per flavor family at runtime. Document with a comment explaining why these remain as inline styles. The test should have an exception for these.

    **today-page.js (3 lines -- Claude's discretion):**
    The borderLeftColor and color assignments use dynamic per-store brand colors from BRAND_COLORS lookup. Two options:
    - Option A (CSS custom property): `todayCard.style.setProperty('--card-brand', color)` with CSS `.today-card { border-left-color: var(--card-brand, var(--brand)); }`. Also handle the color reset (lines 360, 362) by removing the custom property.
    - Option B (keep .style): These are truly dynamic per-store values. Keeping `.style.borderLeftColor = color` is acceptable.
    Choose whichever approach best fits the codebase. If Option B, document the exception in the test.

    **updates-page.js (3 changes -- opportunistic cleanup):**
    - Line 82: `statusEl.style.color = '#2e7d32';` -> Remove any existing status classes first, then `statusEl.classList.add('text-success');`
    - Line 86: `statusEl.style.color = '#c62828';` -> `statusEl.classList.add('text-danger');`
    - Line 91: `statusEl.style.color = '#c62828';` -> `statusEl.classList.add('text-danger');`
    Make sure to clear previous status classes before adding new ones: `statusEl.classList.remove('text-success', 'text-danger');` before each state change.

    Run all tests to confirm GREEN.
  </action>
  <verify>
    <automated>cd custard-calendar && uv run pytest tests/test_inline_style_elimination.py -v && uv run pytest tests/ -v</automated>
  </verify>
  <done>
    - compare-page.js has zero .style.display assignments
    - shared-nav.js has zero .style.display assignments
    - forecast-map.html inline JS uses classList for visibility toggling and tick styling
    - updates-page.js uses .text-success/.text-danger classes instead of .style.color
    - Truly dynamic inline styles in innerHTML strings (forecast-map popup family color, hotspot-dot background) documented as exceptions
    - today-page.js dynamic brand color handled per Claude's discretion (documented either way)
    - All static analysis tests pass
    - Full test suite shows no regressions beyond pre-existing failures
  </done>
</task>

</tasks>

<verification>
- `cd custard-calendar && uv run pytest tests/test_inline_style_elimination.py -v` -- all tests pass
- `cd custard-calendar && uv run pytest tests/ -v` -- no new failures
- `grep -c '\.style\.display' custard-calendar/docs/compare-page.js` returns 0
- `grep -c '\.style\.display' custard-calendar/docs/shared-nav.js` returns 0
- `grep -c '\.style\.fontWeight\|\.style\.color' custard-calendar/docs/forecast-map.html` returns 0 (within script blocks, excluding innerHTML strings)
- `grep -c '\.style\.color' custard-calendar/docs/updates-page.js` returns 0
</verification>

<success_criteria>
- All JS .style.display assignments converted to classList.toggle('hidden') / classList.add/remove('hidden')
- Tick styling in forecast-map.html uses .fronts-tick--confirmed CSS class
- updates-page.js status colors use .text-success / .text-danger CSS classes with state tokens
- Dynamic per-store brand colors in today-page.js and dynamic innerHTML colors in forecast-map.html handled appropriately (either converted or documented as exceptions)
- DTKN-03 requirement fully satisfied: all inline styles in HTML eliminated, all .style.* in JS converted to CSS classes (with documented exceptions for truly dynamic runtime values)
</success_criteria>

<output>
After completion, create `.planning/phases/22-inline-style-elimination/22-02-SUMMARY.md`
</output>
